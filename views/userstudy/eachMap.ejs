<% include ../partial/header %>
<link rel="stylesheet" href="/ucss/common.css">
<% include ../partial/nav %>

<script>
  history.pushState(null, null, location.href);
  window.onpopstate = function(event) {
    history.go(1);
  };
</script>
<%
  var qList = ["본 질문은 두 지도 A, B 중 어떤 지도가 해당 주제에 대해 전체 내용을 잘 표현하고 있는지 평가하는것을 목적으로 합니다.","본 질문은 각 지도 A, B가 타임라인과 관련없는 기사를 얼마나 포함하는지 평가하는 것을 목적으로 합니다.","본 질문은 각 지도 A, B가 중복된 내용의 기사를 얼마나 포함하는지 평가하는 것을 목적으로 합니다.","본 질문은 각 지도 A,B가 유사한 내용의 타임라인을 얼마나 포함하는지 평가하는 것을 목적으로 합니다.","본 질문은 각 지도 A,B에서 타임라인이 교차하는 지점이 적절한지를 평가하는 것을 목적으로 합니다."];
  var linkList = ["/bestMap","/cohMap","/redMap","/redTLMap","/conMap"]

  var descList = ["다음 페이지에서 동일한 주제에 대해 생성된 지도 A와 지도 B가 동시에 주어집니다.\n 지도 A,B 중 해당 주제를 잘 나타내는 지도를 선택해주세요.","다음 두 페이지에 걸쳐 동일한 주제에 대해 생성된 지도 A와 지도 B가 각각 주어집니다.\n 각각 지도 A, B에서 타임라인과 관련되지 않은 기사(노드)를 우클릭을 통해 선택해주세요.","다음 두 페이지에 걸쳐 동일한 주제에 대해 생성된 지도 A와 지도 B가 각각 주어집니다.\n 각각 지도 A, B에서 중복되어 나타나는 기사(노드)를 우클릭을 통해 선택해주세요.","다음 두 페이지에 걸쳐 동일한 주제에 대해 생성된 지도 A와 지도 B가 각각 주어집니다.\n 각각 지도 A, B에서 중복되어 나타나는 타임라인(엣지)을 우클릭을 통해 선택해주세요.","다음 두 페이지에 걸쳐 동일한 주제에 대해 생성된 지도 A와 지도 B가 각각 주어집니다.\n 각각 지도 A, B에서 타임라인이 교차하는 지점이 각각의 타임라인의 속성을 모두 반영하고 있는지를 판단하여 그렇지 않은 교차점의 기사(노드)를 우클릭을 통해 선택해주세요."]
%>

<div class="body_contents">
<div class="container">
  <div class="jumbotron question">
    <h1>QUESTION <%=1+now%>)</h1>
    <h2> <%=qList[now]%></h2>

    <%# <h2>각 맵에 대한 상세 평가 : Map String.fromCharCode('A'.charCodeAt(0)+now)에 대해 평가합니다.
    </h2> %>
    <p><%=descList[now]%></p>

    <div>
      <p>* 지도에서 기사는 노드로 표시되며 노드를 클릭하면 기사 내용이 아래에 나옵니다.
        <br/>
      * 지도는 스크롤을 통해 확대,축소가 가능합니다.
      <br/>* 노드는 드래그앤드랍을 통해 위치를 이동할 수 있습니다.<br/>
      * 질문에 해당하는 기사나 타임라인은 노드와 엣지를 우클릭하여 선택할 수 있습니다.
      <br/>* 선택된 기사와 타임라인을 다시 한번 우클릭하면 선택이 취소됩니다.
      <br/>* 기사의 내용을 잘 읽고 아래 질문에 답해주세요.</p>
    </div>

    <p>평가를 진행하시려면 Next버튼을 클릭하세요.</p>
    <p><a class="btn btn-default btn-lg" href="/userstudy<%=linkList[now]%>" role="button"> Next</a></p>
  </div>

</div>

<% include ../partial/footer %>
