<% include ../partial/header %>
<link rel="stylesheet" href="/ucss/common.css">
<% include ../partial/nav %>

<script>
  history.pushState(null, null, location.href);
  window.onpopstate = function(event) {
    history.go(1);
  };
</script>
<%
  var qList = [" 동일한 주제에 대해 맵 A와 맵 B중 어떤 맵이 전체 내용을 잘 표현하고 있습니까?","타임라인과 관련없는 기사를 모두 고르세요.","중복된 내용을 나타내는 기사를 모두 고르세요.","내용이 중복되는 타임라인을 모두 고르세요.","타임라인이 교차하는 지점 중 각 타임라인들과 연관되지 않는 교차점을 고르세요."];
  var linkList = ["/bestMap","/cohMap","/redMap","/redTLMap","/conMap"]
  console.log(linkList[now])
  console.log(now)
%>

<div class="body_contents">
<div class="container">
  <div class="jumbotron question">
    <h1>QUESTION <%=1+now%>)</h1>
    <h2> <%=qList[now]%></h2>

    <%# <h2>각 맵에 대한 상세 평가 : Map String.fromCharCode('A'.charCodeAt(0)+now)에 대해 평가합니다.
    </h2> %>
    <p>평가를 진행하시려면 Next버튼을 클릭하세요.</p>

    <%

    %>
    <p><a class="btn btn-default btn-lg" href="/userstudy<%=linkList[now]%>" role="button"> Next</a></p>
  </div>

</div>

<% include ../partial/footer %>
