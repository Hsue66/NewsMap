<% include ../partial/header %>
<link rel="stylesheet" href="/ucss/common.css">
<style type="text/css">
      .answers{
          height: 100px;
          overflow-y: scroll;
        }
</style>
<% include ../partial/nav %>
<script>
  history.pushState(null, null, location.href);
  window.onpopstate = function(event) {
    history.go(1);
  };
</script>
<div class="body_contents">
<div class="container">
  <div class="jumbotron question">
    <h1>Question <%=1+now%>.<%=1+idx%>)</h1>
    <h2>'<%=topic%>'에 대해 지도 <%=String.fromCharCode('A'.charCodeAt(0)+idx)%>에서 타임라인이 교차하는 지점 중 각 타임라인들의 속성을 모두 반영하지 못하는 교차점을 고르세요.</h2>
    <%
      var next = "Next";
      if(idx === 1)
        next = "제출하기";
      %>
    <%if(con===1){%>
      <p>연결지점이 없으므로 선택하지 않습니다.
      <%=next%>버튼을 눌러주세요.</p>
    <%}else{%>
    <p>교차하는 지점에 해당하는 노란색 노드를 우클릭하면 선택됩니다.</p>
    <%}%>
    <form action="/sendQ4" method="post">

      <div class="answers" id ="conn">

      </div>
      <input type="hidden" name="Con_articles" id="Con_articles">
      <input type="hidden" name="Con_articlesN" id="Con_articlesN">

      <button class="btn btn-lg"><%=next%></button>
    </form>
  </div>

    <div class="container">
      <div class="cy" id="cy" value="<%=dataset%>">

      </div>
    </div>

    <div class="container" >
      <div class="title text-center">
        <h2 id="title"></h2>
        <h4 id="date"></h4>
      </div>
      <p class="article-content text-center" id="contents">
        뉴스를 클릭하시면 기사를 살펴볼 수 있습니다
      </p>
    </div>
</div>

<script src="/ujs/conncyto.js"></script>
<% include ../partial/footer %>
